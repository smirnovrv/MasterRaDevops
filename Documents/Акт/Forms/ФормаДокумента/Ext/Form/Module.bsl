// @strict-types
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	Если Не ЗначениеЗаполнено(Объект.Грузоотправитель) И ЗначениеЗаполнено(Объект.Организация) Тогда 
		ЗаполнитьГрузоотправителя();
		Модифицированность = Истина;	
	КонецЕсли;

	Если Параметры.Ключ.Пустая() Тогда
		
		Объект.Организация = ПараметрыСеанса.ТекущийПользователь.Организация;
		
		Если ЗначениеЗаполнено(Объект.Основание) Тогда  
		
			Запрос = Новый Запрос;
			Запрос.Текст =
			"ВЫБРАТЬ РАЗРЕШЕННЫЕ
			|	Акт.Ссылка
			|ИЗ
			|	Документ.Акт КАК Акт
			|ГДЕ
			|	Акт.Основание = &Основание";

			Запрос.УстановитьПараметр("Основание", Объект.Основание);

			Если Не Запрос.Выполнить().Пустой() Тогда
				Сообщение = Новый СообщениеПользователю;
				Сообщение.Текст = "Акт уже введен!";
				Сообщение.Сообщить();
				Отказ = Истина;
			КонецЕсли;

		КонецЕсли;
	КонецЕсли;
	
	Если  Не Объект.СуммаДокумента = Объект.Товары.Итог("Всего") Тогда 
		Объект.СуммаДокумента = Объект.Товары.Итог("Всего");
	КонецЕсли;
	
	Если ЗначениеЗаполнено(Объект.Статус) Тогда
		Если Объект.Статус = Перечисления.Статусы.Закрыт Тогда
			Если Не РольДоступна("УУ") И Не РольДоступна("МастерМерч") Тогда
				ЭтаФорма.ТолькоПросмотр = Истина;
			КонецЕсли;
		КонецЕсли;	
	иначе	
		Объект.Статус 		= Перечисления.Статусы.Вработе;
		Модифицированность 	= Истина;
	КонецЕсли;	
КонецПроцедуры

&НаКлиенте
Процедура ПриОткрытии(Отказ)
	ВсегоНДС = Объект.Товары.Итог("СуммаНДС");
КонецПроцедуры

&НаКлиенте
Процедура Расчитать()
	ТекущиеДанные =  Элементы.Товары.ТекущиеДанные;
	ТекущиеДанные.Сумма = ТекущиеДанные.Цена*ТекущиеДанные.Количество;
	Если Строка(ТекущиеДанные.НДС) = "Без НДС" Тогда
		ТекущиеДанные.СуммаНДС = 0;
	иначеЕсли Строка(ТекущиеДанные.НДС) = "18%" Тогда
		Если Объект.НДС_В_томЧисле Тогда
			ТекущиеДанные.СуммаНДС = ТекущиеДанные.Сумма / 118 * 18;
		Иначе
			ТекущиеДанные.СуммаНДС = ТекущиеДанные.Сумма * 0.18;
		КонецЕсли;
	ИначеЕсли Строка(ТекущиеДанные.НДС) = "20%" Тогда
		Если Объект.НДС_В_томЧисле Тогда
			ТекущиеДанные.СуммаНДС = ТекущиеДанные.Сумма / 120 * 20;
		Иначе
			ТекущиеДанные.СуммаНДС = ТекущиеДанные.Сумма * 0.2;
		КонецЕсли; 
	ИначеЕсли Строка(ТекущиеДанные.НДС) = "22%" Тогда
		Если Объект.НДС_В_томЧисле Тогда
			ТекущиеДанные.СуммаНДС = ТекущиеДанные.Сумма / 122 * 22;
		Иначе
			ТекущиеДанные.СуммаНДС = ТекущиеДанные.Сумма * 0.22;
		КонецЕсли;
	ИначеЕсли Строка(ТекущиеДанные.НДС) = "5%" Тогда
		Если Объект.НДС_В_томЧисле Тогда
			ТекущиеДанные.СуммаНДС = ТекущиеДанные.Сумма / 105 * 5;
		Иначе
			ТекущиеДанные.СуммаНДС = ТекущиеДанные.Сумма * 0.05;
		КонецЕсли;
	ИначеЕсли Строка(ТекущиеДанные.НДС) = "7%" Тогда
		Если Объект.НДС_В_томЧисле Тогда
			ТекущиеДанные.СуммаНДС = ТекущиеДанные.Сумма / 107 * 7;
		Иначе
			ТекущиеДанные.СуммаНДС = ТекущиеДанные.Сумма * 0.07;
		КонецЕсли;
	ИначеЕсли Строка(ТекущиеДанные.НДС) = "22%" Тогда
		Если Объект.НДС_В_томЧисле Тогда
			ТекущиеДанные.СуммаНДС = ТекущиеДанные.Сумма / 122 * 22;
		Иначе
			ТекущиеДанные.СуммаНДС = ТекущиеДанные.Сумма * 0.22;
		КонецЕсли;	
	КонецЕсли;
	ТекущиеДанные.Всего	= ТекущиеДанные.Сумма+ТекущиеДанные.СуммаНДС;
	Объект.СуммаДокумента 	= Объект.Товары.Итог("Всего");
КонецПроцедуры	

&НаКлиенте
Процедура ТоварыКоличествоПриИзменении(Элемент)
	Расчитать();
КонецПроцедуры

&НаКлиенте
Процедура ТоварыЦенаПриИзменении(Элемент)
	Расчитать();
КонецПроцедуры

&НаКлиенте
Процедура ТоварыНДСПриИзменении(Элемент)
	Расчитать();
КонецПроцедуры

&НаКлиенте
Процедура ТоварыВсегоПриИзменении(Элемент)
	ТекущиеДанные =  Элементы.Товары.ТекущиеДанные;
	Если ТекущиеДанные.СуммаНДС>0 Тогда 
		Если НЕ ТекущиеДанные.Всего = ТекущиеДанные.Сумма+ТекущиеДанные.СуммаНДС Тогда 
			Сообщить("В строке:"+ТекущиеДанные.НомерСтроки+" (Сумма+Сумма НДС) не равна (Всего)"
			+Символы.ПС+"Для документа с НДС, не должно быть так");
			ТекущиеДанные.Всего = ТекущиеДанные.Сумма+ТекущиеДанные.СуммаНДС;
		КонецЕсли;
	КонецЕсли;	
КонецПроцедуры

&НаКлиенте
Процедура ОрганизацияПриИзменении(Элемент)
	ЗаполнитьГрузоотправителя();
КонецПроцедуры  

&НаСервере
Процедура ЗаполнитьГрузоотправителя()
	Объект.Грузоотправитель = Объект.Организация.Грузоотправитель; 
КонецПроцедуры